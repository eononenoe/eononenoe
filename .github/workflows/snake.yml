name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *" # 매일 자정마다 실행
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Snake Animation
        uses: Platane/snk@master
        with:
          github_user_name: eononenoe
          outputs: |
            ./github-contribution-grid-snake.svg
